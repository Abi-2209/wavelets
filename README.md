# wavelets
Loads a grayscale image and performs 2D wavelet decomposition (db2, level 3) to obtain approximation and detail coefficients. Visualizes The decomposition and extracts statistical features (energy, entropy, mean, std deviation) into a feature vector.
